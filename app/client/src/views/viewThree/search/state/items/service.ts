import { axiosAjax } from '@app/src/config/axios';

/*export const serviceProducts = {
  async getData() {
    try {
      const { data } = await axiosAjax.get(process.env.API_PRODUCTS);
      return hydrateProducts(data);
    } catch(error) {
      throw new HttpResponseError(error);
    }
  }
};*/